fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# --- Python 3.14.4 CadQuery workaround ---
# cadquery on PyPI pins cadquery-ocp to <7.9, but 7.9.3.1.1 is the only
# version with a Python 3.14 wheel. Install cadquery with --no-deps, then
# install its runtime dependencies (below) normally:
#
#   pip install cadquery==2.7.0 --no-deps
#   pip install -r requirements-web.txt
#
# On Python <=3.13 you can use the standard resolver instead:
#   pip install cadquery>=2.4
# -----------------------------------------

cadquery-ocp==7.9.3.1.1
ezdxf>=1.3.0
multimethod<2.0,>=1.11
nlopt<3.0,>=2.9.0
runtype
casadi
path
pyparsing>=3.0.0

trimesh[easy]>=4.0
numpy>=1.24
scipy>=1.10
shapely>=2.0
manifold3d>=3.0
